export interface ProductClassificatorsSchema {
    materialOptions: Array<{ label: string; value: string }>;
    colorOptions: Array<{ label: string; value: string }>;
    packOptions: Array<{ label: string; value: string }>;
}
